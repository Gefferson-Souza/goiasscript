// Arquivo de teste básico para testes automatizados
uai mensagem é "Opa, sô!";
prosa(mensagem);

// Teste de variável numérica  
uai numero é 42;
prosa("O número é: " mais numero);

// Teste de função simples
presta_serviço digaOla(nome) {
  faz_favor "Olá, " mais nome mais "!";
}

uai resultado = digaOla("João");
prosa(resultado);